type: certscanner
scope_name: github.com/elastic/opentelemetry-collector-components/receiver/certscannerreceiver
github_project: elastic/opentelemetry-collector-components

status:
  class: receiver
  stability:
    development: [logs]
  distributions: []
#  codeowners:
#    active: []

tests:
  config:
    interval: 5m
    timeout: 3s
    ports:
      include: [443, 8443]
      scan_all: false
