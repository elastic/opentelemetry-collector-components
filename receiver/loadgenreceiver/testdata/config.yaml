loadgen:

loadgen/logs_invalid_max_replay:
  logs:
    max_replay: -1

loadgen/metrics_invalid_max_replay:
  metrics:
    max_replay: -1

loadgen/traces_invalid_max_replay:
  traces:
    max_replay: -1

loadgen/logs_invalid_max_buffer_size:
  logs:
    max_buffer_size: -1

loadgen/metrics_invalid_max_buffer_size:
  metrics:
    max_buffer_size: -1

loadgen/traces_invalid_max_buffer_size:
  traces:
    max_buffer_size: -1

loadgen/profiles_invalid_max_replay:
  profiles:
    max_replay: -1

loadgen/profiles_invalid_max_buffer_size:
  profiles:
    max_buffer_size: -1
