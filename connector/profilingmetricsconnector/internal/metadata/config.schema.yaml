# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for profilingmetrics metrics.
    type: object
    properties:
      samples.beam.count:
        description: "SamplesBeamCountMetricConfig provides config for the samples.beam.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.classification:
        description: "SamplesClassificationMetricConfig provides config for the samples.classification metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      samples.cpython.count:
        description: "SamplesCpythonCountMetricConfig provides config for the samples.cpython.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.custom_aggregation:
        description: "SamplesCustomAggregationMetricConfig provides config for the samples.custom_aggregation metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.dotnet.count:
        description: "SamplesDotnetCountMetricConfig provides config for the samples.dotnet.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.frame_type:
        description: "SamplesFrameTypeMetricConfig provides config for the samples.frame_type metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      samples.go.count:
        description: "SamplesGoCountMetricConfig provides config for the samples.go.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.jvm.count:
        description: "SamplesJvmCountMetricConfig provides config for the samples.jvm.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.kernel.count:
        description: "SamplesKernelCountMetricConfig provides config for the samples.kernel.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.native.count:
        description: "SamplesNativeCountMetricConfig provides config for the samples.native.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.perl.count:
        description: "SamplesPerlCountMetricConfig provides config for the samples.perl.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.php.count:
        description: "SamplesPhpCountMetricConfig provides config for the samples.php.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.ruby.count:
        description: "SamplesRubyCountMetricConfig provides config for the samples.ruby.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.rust.count:
        description: "SamplesRustCountMetricConfig provides config for the samples.rust.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.user.count:
        description: "SamplesUserCountMetricConfig provides config for the samples.user.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      samples.v8js.count:
        description: "SamplesV8jsCountMetricConfig provides config for the samples.v8js.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for profilingmetrics metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
