type: spanmetricsv2
scope_name: otelcol/spanmetricsconnectorv2

status:
  class: connector
  stability:
    alpha: [traces_to_metrics]
  distributions: []

tests:
  config:
    spans:
      - name: test.metric
        description: "test metric"
