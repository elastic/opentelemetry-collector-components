# Invalid config, should throw an error on validate
spanmetricsv2:
  spans:
    - name: identical.name
      description: Identical metric name
      attributes:
        - key: key.1
      histogram:
        explicit: {}
    - name: identical.name
      description: Identical metric name
      attributes:
        - key: key.1
      histogram:
        explicit: {}
  
