spanmetricsv2:
  spans:
    - name: trace.span.duration
      description: Span duration with custom histogram buckets
      statements:
        value: get(Seconds(end_time - start_time))
      histogram:
        explicit:
          buckets: [0.001, 0.1, 1, 10]
        exponential:
          max_size: 2
