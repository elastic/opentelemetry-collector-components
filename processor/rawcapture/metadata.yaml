type: rawcapture

status:
  class: processor
  stability:
    development: [logs]
  distributions: []
  codeowners:
    active: [joereuter]

tests:
  config:
    attribute_key: raw.id
    extension_name: rawsamplingbuffer
    skip_on_error: false
