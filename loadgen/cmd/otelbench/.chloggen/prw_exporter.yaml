change_type: enhancement

note: "Add `prometheusremotewrite` exporter to the otelbench component registry"

component: otelbench

issues: [1250]

subtext: |
  The Prometheus Remote Write exporter is now registered in otelbench,
  enabling load tests that send metrics directly via Prometheus Remote Write.
