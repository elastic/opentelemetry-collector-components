type: apmconfig

status:
  class: extension
  stability:
    development: [extension]
  distributions: []
  codeowners:
    active: [rogercoll]

tests:
  config:
    opamp:
      protocols:
        http:
          endpoint: localhost:8200
    fetcher:
      elasticsearch:
        endpoint: http://localhost:8200
