type: apmconfig

status:
  class: extension
  stability:
    development: [extension]
  distributions: []
  codeowners:
    active: [rogercoll]

tests:
  config:
    providers:
      elasticsearch:
        endpoint: http://localhost:8200
