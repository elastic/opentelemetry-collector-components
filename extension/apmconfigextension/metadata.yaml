type: apmconfig

status:
  class: extension
  stability:
    development: [extension]
  distributions: []
  codeowners:
    active: [rogercoll]

tests:
  config:
    opamp:
      protocols:
        http:
          endpoint: localhost:8200
    source:
      elasticsearch:
        endpoint: http://localhost:8200
